<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>

  <ItemGroup>
    <!-- Shared platform hosting library — enforces config/pipeline standards -->
    <ProjectReference Include="..\Platform.Hosting\Platform.Hosting.csproj" />
  </ItemGroup>

</Project>
